import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';
import { Toaster } from '@/components/ui/sonner';
import { ThemeProvider } from '@/components/theme-provider';
import { ThemeFavicon } from '@/components/theme-favicon';
// Removed OAuth dependency - using Personal Access Token only

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'GitHub CDN Manager',
  description: 'Transform your GitHub repository into a powerful CDN for free',
  icons: {
    icon: '/git-merge-folder.svg',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          defaultTheme="system"
          storageKey="gitcdn-theme"
        >
          <ThemeFavicon />
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
